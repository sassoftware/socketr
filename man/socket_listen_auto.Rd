% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/socketr.R
\name{socket_listen_auto}
\alias{socket_listen_auto}
\title{Listen on the first available IPv6 or IPv4 address family.}
\usage{
socket_listen_auto(
  address = NULL,
  port = NULL,
  backlog = 128L,
  reuse_address = TRUE,
  cloexec = TRUE,
  prefer = c("inet6", "inet")
)
}
\arguments{
\item{address}{A local hostname or IP address, or \code{NULL} for a wildcard.}

\item{port}{A port between 0 and 65535, or \code{NULL} when embedded in a
bracketed endpoint such as \code{"[::1]:12345"}.}

\item{backlog}{Maximum pending connection queue length.}

\item{reuse_address}{Whether to set \code{SO_REUSEADDR} before binding.}

\item{cloexec}{Whether to set close-on-exec.}

\item{prefer}{Address families to try, in order.}
}
\value{
A listening socketR handle.
}
\description{
Creates a stream listener, tries bind candidates in \code{prefer} order, and
returns the first listener that binds and listens successfully. When
\code{address} is \code{NULL}, the family wildcard (\code{"::"} or \code{"0.0.0.0"}) is used.
This helper supports IPv4 and IPv6 stream sockets only. Unix-domain sockets
require an explicit filesystem path with \code{\link[=socket_create]{socket_create()}} and
\code{\link[=socket_bind]{socket_bind()}}.
}
\examples{
\dontshow{if (.Platform$OS.type != "windows") withAutoprint(\{ # examplesIf}
listener <- socket_listen_auto(port = 0L, prefer = "inet")
socket_local_name(listener)
socket_close(listener)
\dontshow{\}) # examplesIf}
}
