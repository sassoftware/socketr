% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/socketr.R
\name{socket_connect_auto}
\alias{socket_connect_auto}
\title{Connect using the first available IPv6 or IPv4 endpoint.}
\usage{
socket_connect_auto(
  address,
  port = NULL,
  type = c("stream", "dgram"),
  protocol = 0L,
  nonblocking = FALSE,
  cloexec = TRUE,
  prefer = c("inet6", "inet")
)
}
\arguments{
\item{address}{A hostname or numeric IP address.}

\item{port}{A port between 0 and 65535, or \code{NULL} when embedded in a
bracketed endpoint such as \code{"[::1]:12345"}.}

\item{type}{Socket type: \code{"stream"} or \code{"dgram"}.}

\item{protocol}{Numeric protocol, usually zero.}

\item{nonblocking}{Whether the socket should be nonblocking.}

\item{cloexec}{Whether to set close-on-exec.}

\item{prefer}{Address families to try, in order.}
}
\value{
A connected socketR handle.
}
\description{
Resolves \code{address} for both requested address families, tries endpoints in
\code{prefer} order, and creates a socket matching the endpoint that succeeds.
This is the address-aware alternative to an \code{"auto"} socket domain.
It supports IPv4 and IPv6 Internet sockets only; Unix-domain filesystem
paths are not resolved by this helper. Use \code{type = "dgram"} for UDP or the
default \code{type = "stream"} for TCP.
}
\examples{
if (interactive()) {
  client <- socket_connect_auto("localhost", 80L)
  socket_close(client)
}
}
