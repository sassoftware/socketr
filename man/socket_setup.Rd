% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/socketr.R
\name{socket_setup}
\alias{socket_setup}
\alias{socket_create}
\alias{socket_bind}
\alias{socket_listen}
\alias{socket_accept}
\alias{socket_connect}
\title{Create and configure POSIX sockets.}
\usage{
socket_create(
  domain = c("inet", "inet6", "unix"),
  type = c("stream", "dgram"),
  protocol = 0L,
  nonblocking = FALSE,
  cloexec = TRUE
)

socket_bind(socket, address = NULL, port = NULL)

socket_listen(socket, backlog = 128L)

socket_accept(socket, nonblocking = NULL)

socket_connect(socket, address, port = NULL)
}
\arguments{
\item{domain}{Address family: \code{"inet"}, \code{"inet6"}, or \code{"unix"}.}

\item{type}{Socket type: \code{"stream"} or \code{"dgram"}.}

\item{protocol}{Numeric protocol, usually zero.}

\item{nonblocking}{Whether the accepted socket should be nonblocking; \code{NULL}
inherits the listener mode.}

\item{cloexec}{Whether to set close-on-exec.}

\item{socket}{A socketR socket handle.}

\item{address}{A hostname, IP address, or Unix-domain path.}

\item{port}{A port between 0 and 65535; \code{NULL} for Unix sockets.}

\item{backlog}{Maximum pending connection queue length.}
}
\value{
A socketR external-pointer handle.

Invisibly, the socket handle.

Invisibly, the socket handle.

A socket handle, or \code{NULL} when no connection is pending.

\code{TRUE} when connected, or \code{FALSE} when connection is in progress.
}
\description{
Create a POSIX socket.
}
\examples{
s <- socket_create()
socket_close(s)
}
