% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/socketr.R
\name{socket_connections}
\alias{socket_connections}
\alias{socket_connection}
\alias{socket_connection_read}
\alias{socket_connection_write}
\title{Adapt sockets and R connections for byte I/O.}
\usage{
socket_connection(socket, close_socket = FALSE)

socket_connection_read(connection, n = 4096L)

socket_connection_write(connection, data)
}
\arguments{
\item{socket}{A socketR socket handle.}

\item{close_socket}{Whether closing the adapter should also close the
underlying socket. The default \code{FALSE} preserves adapter-only close
behavior.}

\item{connection}{An open R connection.}

\item{n}{Maximum number of bytes to read.}

\item{data}{A raw vector.}
}
\value{
An R \code{connection} object.

A raw vector, possibly shorter than \code{n} at end-of-file.

Number of bytes written.
}
\description{
Adapt a socket handle to an R connection.
The returned connection can be passed to base R functions such as
\code{readBin()}, \code{writeBin()}, \code{readLines()}, and \code{writeLines()}. Closing the
connection does not close the underlying socket handle; call \code{\link[=socket_close]{socket_close()}}
explicitly when the handle is no longer needed.

Uses R's connection dispatch, so this works with \code{\link[=socket_connection]{socket_connection()}} and
other readable R connections.

Uses R's connection dispatch, so this works with \code{\link[=socket_connection]{socket_connection()}} and
other writable R connections.
}
\examples{
s <- socket_create()
con <- socket_connection(s)
close(con)
socket_close(s)
}
